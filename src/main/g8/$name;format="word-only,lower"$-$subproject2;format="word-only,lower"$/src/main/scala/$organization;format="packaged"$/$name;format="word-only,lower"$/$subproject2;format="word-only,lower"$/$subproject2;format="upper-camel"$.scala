package $organization$.$name;format="word-only,lower"$.$subproject2;format="word-only,lower"$

class $subproject2;format="upper-camel"$ {
  def greet(name: String): String = {
    s"hello, \$name"
  }
}
