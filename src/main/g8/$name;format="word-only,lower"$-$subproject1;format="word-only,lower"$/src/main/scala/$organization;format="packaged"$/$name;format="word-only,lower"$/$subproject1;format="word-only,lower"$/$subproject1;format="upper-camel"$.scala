package $organization$.$name;format="word-only,lower"$.$subproject1;format="word-only,lower"$

class $subproject1;format="upper-camel"$ {
  def greet(name: String): String = {
    s"hello, \$name"
  }
}
